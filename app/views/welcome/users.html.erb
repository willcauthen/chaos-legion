<div class="container">
	<div class="row">
		<div class="col-xs-3"></div>
		<div class="col-xs-6 jumbotron">
			<ul>
				<%  @users.each do |user| %>
				<li>Name: <%= user.name %> -- <%= user.score %></li>
				<%= button_to 'Increment', root_path, :class => 'btn btn-primary' %>
				<hr>
				<% end %>
			</ul>			
					</div>
		<div class="col-xs-3"></div>
	</div>
</div>

<hr>